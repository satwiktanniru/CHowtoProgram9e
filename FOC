#include <stdio.h>

int main() {
   int n, i;
   printf("Enter a value for n: ");
   scanf("%d", &n); // taking input for n

   for(i=1; i<=n; i++) {
      printf("%d ", i); // printing the sequence of numbers
   }

   return 0;
}
